apiVersion: v1
kind: Service
metadata:
  name: {{ include "s3www-sources.fullname" . }}
  labels:
    {{- include "s3www-sources.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "s3www-sources.selectorLabels" . | nindent 4 }}
